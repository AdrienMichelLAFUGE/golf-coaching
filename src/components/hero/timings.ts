// Shared timing constants for the hero narrative + orbit morph.
// `p` is scroll progress in [0,1] from `useScroll({ target: heroRef, offset: ["start start","end start"] })`.

export const MORPH_START = 0.18;
export const MORPH_END = 0.68;

export const TABLET_OPACITY_START = 0.22;
export const TABLET_OPACITY_END = 0.60;
